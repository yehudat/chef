- run ./chef.zsh as it runs chef.py in a docker container
- run ./test.zsh every time you complete the work on the code, but before commits
- git comments messages shall have 2 tags and max-6 words comment. The first tag 
is always repo dir name, the other the essense of the feature in one word.
Example: [chef] [renderer] fix table header column names
